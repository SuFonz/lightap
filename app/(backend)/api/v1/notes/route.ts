export const dynamic = "force-dynamic";

interface Body {
    context: string,
    replyTo: string,
}

export async function POST(request: Request) {
    // 解析参数

    // 验证 JWT

    // 存入数据库

    // 递送
}
